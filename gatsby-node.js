/*var path = require('path');

exports.rewritePath = function (parsedPath, pageData){
  console.log(path.join('/', parsedPath.dir, encodeURIComponent(parsedPath.name), '/'));
  console.log('page path: ', pageData.path);
  if(parsedPath.ext === 'md'){
    return path.join('/', parsedPath.dir, encodeURIComponent(parsedPath.name), '/');
  }
}*/
